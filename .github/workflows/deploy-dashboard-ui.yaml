name: deploy-dashboard-ui

on:
  workflow_dispatch:
    inputs:
      environment:
        required: true
        description: Deploy to DEV/PROD
        type: choice
        options: [DEV, PROD]

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yaml
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    secrets: inherit
    with:
      dockerfile_path: 'dashboard-ui'
      context_path: '.'
      image_name: 'dishdash-dashboard'
      environment: ${{ github.event.inputs.environment }}
      secret-service-hash: ${{ github.event.inputs.environment == 'PROD' && 'DASHBOARD_UI_HASH' || 'DASHBOARD_UI_HASH_DEV' }}
