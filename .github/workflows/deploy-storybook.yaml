name: deploy-storybook

on:
  workflow_dispatch:
    inputs:
      environment:
        required: true
        description: Deploy to DEV/PROD
        type: choice
        options: [DEV, PROD]

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yaml
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    secrets: inherit
    with:
      dockerfile_path: 'miniapp/.storybook'
      context_path: 'miniapp'
      image_name: 'dishdash-storybook'
      environment: ${{ github.event.inputs.environment }}
      secret-service-hash: ${{ github.event.inputs.environment == 'PROD' && 'STORYBOOK_SERVICE_HASH' || 'STORYBOOK_SERVICE_HASH_DEV' }}
